/** @type {import('next').NextConfig} */
// const nextConfig = {

      
// };

// export default nextConfig;

// next.config.mjs

const nextConfig = {
    images: {
      domains: ['programmerhub.pythonanywhere.com'],
    },
  };
  
  export default nextConfig;
  